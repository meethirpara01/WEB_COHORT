import React from 'react'

const recipe = ( {ing} ) => {
    console.log("Hello From Recipe Componentes!");
    
  return (
    <div className=' text-5xl'>recipe</div>
  )
}

// export default React.memo(recipe);
export default React.memo(recipe);